GrowPoint — Website Review & Fix Report
=======================================
Date: 2026-06-07
Reviewer: automated pass (review #2, includes fixes)

Targets:
  - Live site (GitHub Pages):  https://www.growpoint.bg
  - CloudFront test domain:    https://d30m6jtjij7col.cloudfront.net
  - Live API:                  https://zmajj05nm1.execute-api.eu-west-1.amazonaws.com
  - AWS account:               010419877195 (eu-west-1)
  - Local repo:                /Users/privileged/Projects/growpoint.bg/growpoint.bg

Scope notes from the owner (applied to this pass):
  - Apex DNS (growpoint.bg without www) failing TLS is KNOWN and will be fixed
    later. Excluded from findings.
  - Payments are intentionally not ready. Excluded.
  - Demo/example consultants are kept on purpose so the site looks full for
    pre-launch testing. They were NOT removed — instead their data was repaired.


==================================================================
PART 1 — WHAT WAS TESTED
==================================================================

A) Code & infra validation (local)
   - tsc --noEmit (frontend type-check) ............ PASS
   - node -c backend/api/index.cjs ................. PASS
   - node --check scripts/site-build.mjs .......... PASS
   - node --check scripts/refresh-example-availability.cjs ... PASS
   - terraform validate (infra/terraform) ......... PASS ("Success! The configuration is valid.")
   - terraform fmt -check ......................... clean

B) Safe production smoke (npm run smoke:prod) — twice (before & after fixes)
   - 14/14 PASS both runs (latest run id 6u51r3nfog).
   - Covers: API /health, /consultants (11 profiles), Cyrillic profile route,
     /, /users/, /consultants/, a profile route, /auth/, sitemap, robots,
     anonymous 401 on /me/notifications, /bookings, /admin/consultants,
     and production-origin CORS preflight (204).

C) Live API probing (curl)
   - GET /health -> {"ok":true,"service":"growpoint-api"}
   - GET /consultants -> 11 items (10 demo isExample=true + 1 real "Димитър Менторски")
   - GET /consultants/{cyrillic-slug} -> 200 with decoded Bulgarian content
   - Anonymous GET on /me/profile, /me/notifications, /bookings,
     /admin/consultants, /bookings/{id}/messages -> all 401 (no data leak)
   - POST /me/documents/{id}/share (no token) -> 404 (route doesn't exist;
     sharing is an embedded field saved via PUT /me/profile — correct)
   - OPTIONS /bookings from Origin www.growpoint.bg -> 204

D) Browser render (local dev server via Preview, production env mode)
   - Home page renders cleanly; no JS console errors.
   - Mobile (375px) + responsive layout: hamburger menu, hero, cookie banner
     all render correctly.
   - Cookie consent defaults to privacy-preserving copy ("Само необходимите"
     / "Приемам"); no third-party ad trackers mentioned.
   - "beta" warning tape banner is present and dismissible.
   - NOTE: API-backed pages (consultant detail, directory) cannot load from
     localhost because the API CORS allowlist only contains www.growpoint.bg
     and the CloudFront origins. This is a dev-only limitation, NOT a bug —
     the production origin is allowlisted and works.

E) Per-route SEO metadata (live HTML + regenerated build)
   - Every public route returns a route-specific <title>, description,
     canonical, OG/Twitter tags and JSON-LD. Cyrillic slug canonical URLs are
     correctly percent-encoded.
   - CSP, referrer-policy, robots meta, hreflang, structured data all present.

F) Build pipeline
   - npm run build (GitHub Pages) ....... OK; regenerated route copies + sitemap
   - npm run build:cloudfront ........... OK; now also emits per-route HTML into dist/
   - Bundle (gzip): main JS 154 KB, HeroAnimation 125 KB (separate lazy chunk),
     CSS 27 KB. Healthy — the raw ~540 KB figure noted in review #1 was the
     uncompressed size; gzip transfer size is fine and the hero animation is
     correctly code-split.


==================================================================
PART 2 — WHAT WAS FIXED IN THIS PASS
==================================================================

[DONE + LIVE] FIX 1 — Stale demo availability (was review #1 finding F3)
   Problem: all 10 demo consultants had availability entirely in May 2026 (in
   the past). The booking panel filters to future slots only, so every demo
   profile showed "Свободните часове се подготвят" with zero bookable hours —
   the opposite of "looks full".
   Fix: added scripts/refresh-example-availability.cjs. It scans the
   consultants table, and for rows with isExample === true ONLY, rewrites
   `availability` to future weekday slots (next ~2 weeks, weekends skipped),
   preserving each profile's original hour-of-day pattern, and recomputes
   `nextAvailable`. Real profiles are never touched. Safe-by-default (dry-run
   unless --apply).
   Applied to production DynamoDB (owner-approved). Verified via live API:
   all 11 public profiles now have a future nextAvailable (earliest 2026-06-08)
   and 10–40 future slots each. This is live immediately (data is fetched at
   runtime), no redeploy required.
   Reversible: DynamoDB point-in-time recovery is enabled; re-runnable any time.

[DONE — staged, not yet deployed] FIX 2 — og:image leaked external demo URLs (F4)
   Problem: demo profiles' social/SEO images pointed at randomuser.me stock
   photos (e.g. og:image = randomuser.me/...men/32.jpg). README intent is that
   SEO/social images stay branded/stable.
   Fix: scripts/site-build.mjs now only trusts images hosted on the GrowPoint
   site host or *.amazonaws.com for SEO. External/demo hosts and signed S3 URLs
   fall back to the branded default (assets/advertisement/3.jpg).
   Verified: regenerated the committed GitHub Pages route HTML — og:image,
   twitter:image AND JSON-LD ImageObject for all 10 demo profiles now use the
   branded image. The real Менторски profile was already branded (unchanged).
   STATUS: the fix is staged in the working tree (consultants/*/index.html +
   sitemap.xml). It goes live only after commit + push to the GitHub Pages
   branch. Not pushed (left to the owner).

[DONE — staged, not deployed by owner choice] FIX 3 — CloudFront per-route SEO (F1)
   Problem: CloudFront served the generic home <title> for ALL routes,
   including known consultant slugs. Two root causes:
     (a) build:cloudfront did not render per-route static HTML into dist/.
     (b) The distribution uses a private S3 REST origin (OAC). default_root_object
         only resolves "/", so "/consultants/foo/" hit S3 as a missing key,
         got 403/404, and the custom_error_response fell back to the generic
         root /index.html — losing per-route metadata.
   Fix (two parts):
     (a) scripts/site-build.mjs: build:cloudfront now renders the per-route
         index.html files into dist/ (renderStaticRoutes: true). Verified —
         dist/consultants/<slug>/index.html etc. are emitted with correct
         per-route metadata.
     (b) infra/terraform/main.tf: added a CloudFront Function (viewer-request,
         cloudfront-js-2.0) that rewrites directory-style URIs to their
         index.html object ("/x/" -> "/x/index.html", "/x" -> "/x/index.html",
         leaving files with extensions alone), and associated it with the
         default cache behavior. Unknown routes still 404 at S3 and fall back to
         the SPA shell, so SPA fallback is preserved. terraform validate passes.
   STATUS: NOT applied to AWS (owner chose to keep this local for now). To
   deploy: see PART 4.


==================================================================
PART 3 — WHAT STILL NEEDS WORK (remaining)
==================================================================

[P0] Deploy the CloudFront SEO fix before DNS cutover.
   The code/infra above is ready but not applied. Until then, CloudFront still
   serves generic metadata per route. Apply steps in PART 4. (Apex DNS itself
   is owner-acknowledged/out of scope.)

[P0] GrowPoint SES email sender.
   Still blocked on Cloudflare SES DNS records + SES production access. Real
   verification/reset/booking/admin/reminder email content has never been seen
   in a real inbox; backend falls back to in-app notifications. (Cognito's
   default sender still delivers signup/reset codes.)

[P1] Admin browser flow (approve / reject / delete / preview / message).
   Not exercised in this pass (no admin credentials used here). The featured
   ("Подбрани") API and UI are implemented per the README ledger but the
   destructive admin actions still need a browser pass with one disposable
   consultant.

[P1] Documents-sharing UI on desktop + mobile.
   API privacy is verified (403 for unrelated consultant). The visible
   dashboard upload/share flow still needs a real-user pass.

[P1] Mobile logged-in UX (dashboard, notifications, messages, booking actions).
   Only the public home/mobile shell was checked here (logged-in pages need
   the live origin / real auth).

[P2] Reminder timing around a real upcoming session — observe live.

[P2] Sitemap/SEO freshness automation.
   site-build runs only at build time, so a newly approved consultant won't
   appear in sitemap.xml / get a route copy until the next build+deploy.
   Consider a scheduled CloudFront build/deploy (or edge metadata renderer)
   after profile approvals.

[P2] Observability: CloudWatch alarms, rate limits, structured validation
   tests, and load testing are still thin for scale.

DOC NIT: README section 7.2 route map omits the document-sharing mechanism
   (it's an embedded `sharedWithConsultantIds` field saved via PUT /me/profile,
   not a dedicated /share route). Worth a one-line note so future readers don't
   look for a missing endpoint.


==================================================================
PART 4 — HOW TO DEPLOY THE STAGED FIXES
==================================================================

1) og:image fix (GitHub Pages):
   - Already rebuilt into the working tree. Review `git diff` on
     consultants/*/index.html and sitemap.xml, then commit + push the
     GitHub Pages source branch. (The owner controls commit/push.)

2) CloudFront per-route SEO fix:
   - terraform -chdir=infra/terraform apply        # creates the CF Function + association
   - npm run deploy:cloudfront                      # builds dist (now with route copies) and syncs to S3 + invalidates
   - node scripts/smoke-production.mjs --cloudfront # confirm routes + SPA fallback
   - Manually confirm a known route now serves its own <title> on the
     CloudFront domain, e.g.:
       curl -s https://d30m6jtjij7col.cloudfront.net/consultants/georgi-dimitrov-tech-careers/ | grep '<title>'

3) Demo availability:
   - Already applied & live. Re-run any time it goes stale again:
       AWS_REGION=eu-west-1 CONSULTANTS_TABLE=careerdoc-dev-consultants \
         node scripts/refresh-example-availability.cjs --apply


==================================================================
PART 5 — FILES CHANGED IN THIS PASS
==================================================================
   scripts/site-build.mjs                  (og:image host allowlist; CF route copies)
   infra/terraform/main.tf                 (CloudFront Function + association)
   scripts/refresh-example-availability.cjs (NEW — demo availability refresher)
   consultants/*/index.html, sitemap.xml   (regenerated GitHub Pages artifacts w/ branded og:image)
   test.txt                                (this report)
   .claude/launch.json                     (NEW — local Preview dev-server config; harmless, optional)

Production data changed: availability/nextAvailable on the 10 isExample
consultants in careerdoc-dev-consultants (owner-approved; PITR-reversible).
No commit/push and no Terraform/CloudFront deploy were performed.


==================================================================
PART 6 — SUMMARY
==================================================================
The platform's public read paths, API security boundaries, CORS, SEO metadata,
and build/infra config are all healthy and validate cleanly.

This pass repaired the three substantive content/SEO issues from review #1 that
were in scope:
  • Demo availability is fixed and LIVE — the directory now shows real,
    upcoming bookable slots on every profile.
  • Demo og:image leakage is fixed in the build (staged; commit+push to deploy).
  • The CloudFront per-route SEO regression is fixed in code + Terraform
    (staged; apply+deploy to activate) — this clears the main blocker for the
    eventual DNS cutover.

Out of scope by owner direction: apex DNS/TLS, payments, demo-profile removal.
Still genuinely open: SES email, admin destructive-action browser QA, logged-in
mobile UX, reminder timing, and scale observability.


==================================================================
SPRINT: PRODUCTION READINESS (BG launch) — PROGRESS & BUG LOG
==================================================================
Date: 2026-06-07 (sprint pass)

AUDIT RESULTS (grounding the plan)
  - Demo "example" label: PRESENT. <ExampleBadge> ("Пример", tooltip "Този
    профил е примерен и служи за визуализация.") renders on spotlight, directory
    cards, and the public profile (SiteAppLegacy.tsx:905/1900/4508/6586).
    Requirement met; still want a live visual confirm.
  - Bulgarian completeness: GOOD. UI is overwhelmingly BG. Only minor English in
    a few input PLACEHOLDERS that are intentional anglicisms/examples
    (e.g. "MBA, Product School, Google Analytics", "Stakeholder management, CV
    writing, Interview prep", slug placeholder "ivan-petrov"). Not blockers.
  - Repo security: CLEAN. No AWS keys / private-key blocks in tracked files.
    terraform.tfvars (real social-login secrets) is gitignored; only
    terraform.tfvars.example + docs/social-login-setup.md contain placeholders.
    .env.production is tracked but holds ONLY public client config (API URL +
    Cognito pool/app-client IDs) that already ships in the frontend bundle.
  - Payment groundwork: PARTIAL. CSS for .upgrade-preview-card (client +
    consultant variants) exists but appears unwired in the React tree — no
    visible post-registration upgrade hint yet.

PERF / RELIABILITY FINDINGS (measured against live API)
  [BUG-P1] Public /consultants is slow: TTFB ~1.9s, total ~2.6s for 31 KB.
    Single profile TTFB ~1.0s. Likely Lambda cold start + list work. Caching
    helps repeat/concurrent loads; cold-start latency still needs attention
    (warm path / lighter payload / GSI query) for a snappy first load.
  [BUG-P1] Public endpoints sent NO cacheable header (Cache-Control: no-store) —
    every load hit Lambda (cost + latency). README claimed cache headers exist;
    it did not match the code.
  [BUG-P2] Demo avatars are hosted on randomuser.me (TTFB ~2s, external, not
    under our control) — slow profile pictures and a third-party dependency.
    Real avatars are 1-hour signed S3 URLs (not CDN-cached).

FIXES MADE THIS SPRINT (staged locally; not deployed/committed)
  [DONE] Speed: added "Cache-Control: public, max-age=60,
    stale-while-revalidate=300" to GET /consultants and /consultants/{slug}
    (backend/api/index.cjs). Verified signed media URLs live 3600s, comfortably
    longer than the max served age, so cached responses never serve an expired
    image URL. Profile edits still surface within ~60s. Deploy via terraform
    apply to take effect.
  [DONE] Security: hardened .gitignore — ignores .env / .env.* (keeps public
    .env.production), .env*.local, *.pem/*.key/*.p8/*.p12/*.pfx, id_rsa*, .aws/,
    aws-credentials*, service-account*.json. Verified .env.production stays
    tracked and stray secrets (terraform.tfvars, backend/api/.env, *.p8, *.pem)
    are now ignored.

BLOCKED — NEEDS OWNER AUTHORIZATION
  - Functional QA of registration / forgot-password / admin approval requires
    creating real (self-cleaning) accounts in production Cognito via
    `npm run smoke:prod:live`. The safety layer blocked this pending explicit
    approval. This is the core "test every flow + record bugs" item (#12) and is
    not yet done.

FUNCTIONAL QA RESULT (npm run smoke:prod:live, run yhg62ix3wwl) — owner-approved
  PASSED 27/27. No functional bugs found. Verified end-to-end against live prod:
    - Signup + resend confirmation + admin confirm (2 disposable users)
    - Login (client + consultant tokens), bootstrap, profile creation
    - Consultant profile save + approval + public route
    - Avatar + cover upload (signed S3)
    - Booking lifecycle: create, accept, decline, cancel, reschedule, re-accept
    - Booking messages BOTH directions (client↔consultant)
    - ICS download, document upload/share/privacy (incl. unrelated-user 403)
    - Past-session confirmation + review (rating 5)
    - Notifications list + mark-read
    - Forgot-password request delivery
    - Disposable production data cleaned up afterward
  SKIP: optional admin auth/message test (needs GROWPOINT_ADMIN_EMAIL/PASSWORD —
    admin approve/reject/delete BROWSER flow still not exercised here).

SPRINT CLOSE-OUT (deployed)
  Owner approved the live functional test + a full deploy. Shipped:
  - Backend cache headers: LIVE. GET /consultants + /consultants/{slug} now
    return "public, max-age=60, stale-while-revalidate=300". Warm list TTFB
    dropped ~1.9s -> ~0.42s. (terraform apply, commit d2c57c6f)
  - CloudFront per-route SEO: LIVE on the test domain. The new CloudFront
    Function rewrites directory paths to index.html, so known routes serve
    route-specific <title>/metadata (verified: georgi, Cyrillic Менторски,
    about) while unknown routes still return the SPA shell and assets are
    untouched. (terraform apply + npm run deploy:cloudfront)
  - .gitignore hardening + README security/prod notes: committed d2c57c6f.
  - og:image branded fix + demo availability refresh: were committed/pushed by
    the owner as da4b5114 and are live.
  - Payment groundwork: post-registration "GrowPoint Pro" upgrade hint added to
    the dashboard for free-plan users (role-aware, BG copy, "payments not active
    yet", CTA -> /contact). Pushed b0e2f68b. NOTE: compiles + builds + app boots
    clean, but the dashboard is auth-gated so it has NOT had a logged-in visual
    pass yet — quick visual confirm recommended.
  - Post-deploy CloudFront smoke: 14/15 (the 1 fail was a transient "fetch
    failed"; /bookings re-verified 401 x3 — not a bug).

  STATUS BY TASK:
    #12 Functional QA ............ DONE (27/27, no functional bugs)
    #13 Repo security/.gitignore . DONE + committed
    #14 Speed (cache headers) .... DONE + deployed (image-CDN still a P2 idea)
    #15 Demo label + Bulgarian ... DONE (badge present; BG essentially complete)
    #16 Payment groundwork ....... DONE + deployed (visual check pending)
    #17 Messaging/notifications .. VERIFIED server-side (two-way messages +
        notification counts passed in 27/27); mobile visual pass still open
    #18 README ................... DONE + committed

  REMAINING (carry-over, not blockers for a soft launch):
    - Admin approve/reject/delete BROWSER flow (set GROWPOINT_ADMIN_* and test)
    - Logged-in MOBILE visual pass (dashboard, upgrade card, messages, bookings)
    - SES production sender + real inbox email copy (Cloudflare DNS + SES access)
    - Profile-picture CDN / stable public derivatives (demo avatars still on
      external randomuser.me ~2s; real avatars are 1h signed S3 URLs)
    - Cold-start API latency (~1-2s first hit) — consider warmer/lighter path
    - Repo bloat: backend/api/node_modules is committed (P2 hygiene)
    - Apex DNS/TLS cutover (owner-owned, out of scope)


==================================================================
FOLLOW-UP SESSION — admin test, cleanup, network, CloudFront/DNS
==================================================================
Date: 2026-06-07 (later)

ENVIRONMENT NOTE (root cause of the Terraform error)
  The owner's `terraform plan` failed with "dial tcp: lookup
  sts.eu-west-1.amazonaws.com: no such host". This is the LOCAL MACHINE's
  network/DNS to AWS flapping (probes alternated HTTP 200 / HTTP 000 / hung),
  NOT a Terraform or config problem. Public-domain DNS is unrelated and fine:
  growpoint.bg is registered/DNS-managed at **jethost.bg** and resolves `www`
  to GitHub Pages (the live landing page). Fix on the machine: reconnect
  network/VPN, set a working resolver (e.g. 1.1.1.1 / 8.8.8.8), confirm with
  `nslookup sts.eu-west-1.amazonaws.com`, then re-run terraform.

ADMIN ACCOUNT TEST (admin@admin.com)
  - Admin Cognito login (USER_PASSWORD_AUTH): WORKS.
  - GET /admin/consultants (admin-only list): WORKS — returned 18 profiles
    incl. non-public/pending/rejected ones.
  - PUT /admin/consultants/{id}/status -> rejected: WORKS (verified live;
    statusUpdatedByEmail=admin@admin.com, isPublic flipped to false).
  - NOT yet exercised: approve (re-approve), featured toggle on a disposable
    profile, admin message, and the full lifecycle via smoke:prod:live — the
    machine network was too unstable to run the full suite safely this session.

  [BUG-P0 FOUND + FIXED] Orphaned QA profile leaked to the PUBLIC directory:
    "Codex QA Mentor lx9tnahos1o" (consultant-4cead84c-...) was approved+public
    and appeared in live GET /consultants (public count had risen 11 -> 12). It
    is an orphan from a smoke run whose cleanup failed when the network dropped.
    FIX: admin-rejected it (reversible) with owner approval -> isPublic=false,
    removed from the public directory (allow up to ~60s for the new cache TTL).

  [NOTE] 6 other non-public test profiles remain in the DB by owner's choice
    (keep): gsgdsgds (pending), Dimitar Adminski x2 (approved+rejected),
    testtesttesttest (approved), Димитър Консултантски (rejected), Пепи Еврото
    (active). None are visible on the public site (public filter = approved +
    complete + not self-approved).

  PROCESS GAP: smoke:prod:live cleanup is not resilient to mid-run network
    failure -> it can orphan a public consultant. Consider an idempotent
    "purge by run-id / purge codex-* orphans" cleanup pass, and run the live
    suite only on a stable connection.

CLOUDFRONT — SPEED / COST FOR A BULGARIAN AUDIENCE
  - Price class already = PriceClass_100 (North America + EUROPE) — the cheapest
    class that still serves Bulgaria from EU edge locations. Cost-optimal; no
    change needed.
  - Public API caching added earlier (max-age=60, SWR=300) already cuts repeat
    latency and Lambda cost.
  - Live site today is GitHub Pages (Fastly CDN) — already fast for BG; it is
    NOT slow. "Using CloudFront" = the DNS cutover, which is a jethost.bg action
    (+ needs AWS access, currently network-blocked).
  - Doc fix: ~14 stale "Cloudflare" references corrected to "jethost.bg" across
    README + variables.tf, plus an explicit DNS note. (Uncommitted — commit when
    the network/classifier recover.)
  - Still a real speed item: demo avatars load from external randomuser.me
    (~2s); self-hosting them or using stable S3/CloudFront-served derivatives
    would speed up profile pictures (needs a DynamoDB write -> deferred).

UNCOMMITTED LOCAL CHANGES (commit when network is stable)
  - README.md (Cloudflare -> jethost.bg + DNS note)
  - infra/terraform/variables.tf (Cloudflare -> jethost.bg)
  - test.txt (this section)


==================================================================
SESSION: SOCIAL LOGIN + UI POLISH (Google/LinkedIn live)
==================================================================
Date: 2026-06-08

SHIPPED & DEPLOYED (GitHub Pages + CloudFront)
  - Google + LinkedIn social login: LIVE via Cognito hosted UI.
      * Cognito hosted-UI domain growpoint-auth.auth.eu-west-1.amazoncognito.com
        created (terraform apply) and ACTIVE; Google + LinkedInOIDC identity
        providers attached to pool eu-west-1_Yl8EDZKFA (app client
        dtjsjnq6s133c1b04s56sorba).
      * Each provider's redirect URI in its own console/app:
        https://growpoint-auth.auth.eu-west-1.amazoncognito.com/oauth2/idpresponse
      * .env.production: VITE_COGNITO_DOMAIN + VITE_COGNITO_SOCIAL_PROVIDERS=Google,LinkedIn.
      * Fixed resolveAuthRedirectUrl to a stable app-root URL so login works
        from any route (avoids redirect_uri_mismatch).
      * Secrets (Google/LinkedIn client secrets) live ONLY in gitignored
        infra/terraform/terraform.tfvars; verified never committed.
  - Social onboarding modal: new social accounts get a one-time dashboard
    popup (confirm name, upload photo prefilled from provider, city/occupation;
    role defaults to client). Flag: localStorage careerdoc.social-onboarding-pending.
  - Dark theme: fixed unreadable dashboard/profile/auth/modal surfaces that
    hardcoded white/light backgrounds (profile-setup nav, checkbox-row,
    pattern-chip, modal-card, choice-card, availability/dashboard cards,
    auth-progress, faq).
  - Beta "caution tape" banner: removed site-wide (JSX + state + CSS). It had
    also been intercepting clicks on the вход/регистрация tabs (pointer-events).
  - Consultant banner: object-fit cover so vertical uploads no longer leave
    gaps; consolidated conflicting .media-preview-card__cover rules; upload
    field now states recommended size (1600x700, landscape). Verified fill with
    a vertical test image in the preview browser.
  - Repo hygiene: untracked dist/ (now gitignored); added lifecycle
    ignore_changes to the Google/LinkedIn Cognito IdPs so terraform plan is
    clean (no phantom perpetual diff). client_id/secret stay tracked.

VERIFICATION
  - npm run smoke:prod: 14/14 PASS (run zg47az1g0n and others).
  - Live OAuth handoff (curl /oauth2/authorize): Google -> accounts.google.com,
    LinkedIn -> linkedin.com, both with no redirect_uri error.
  - tsc --noEmit, terraform validate, terraform plan (No changes): all pass.
  - CloudFront + www serve the rebuilt bundles (verified bundle hashes/strings).

STILL NEEDS OWNER VERIFICATION (cannot test without a real sign-in / login)
  - Real Google AND LinkedIn end-to-end sign-in on www.growpoint.bg, incl. the
    onboarding modal appearing for a brand-new account.
  - Dark-mode /dashboard visual check (logged-in pages can't load from
    localhost due to the production-only API CORS allowlist).
  - LinkedIn app: confirm "Sign In with LinkedIn using OpenID Connect" product
    is enabled (needed for openid/profile/email scopes).


==================================================================
SPRINT: UI CONSISTENCY + ROLES (sprint-planning + execute)
==================================================================
Date: 2026-06-08 (later)

SHIPPED
  [Roles/groups] Added a `consultants` Cognito group (terraform). /auth/bootstrap
    now treats group membership as authoritative: a member is forced to
    role=consultant and gets a consultant draft, even if they signed up as a
    client. This answers "how do I make a Cognito user a mentor" -> add them to
    the consultants group; they pick it up on next login. `admin` stays separate.
    Deployed via terraform apply (consultants group created, Lambda updated;
    /health still ok). client/secret unaffected.
      To promote: aws cognito-idp admin-add-user-to-group --user-pool-id
        eu-west-1_Yl8EDZKFA --username <email> --group-name consultants
  [Dark theme] Fixed the personal-profile white-background bug: .dashboard-sidebar
    (+ --client/--consultant), .documents-zone, .dashboard-overview--client/
    consultant, .dashboard-sidebar__stats>div, .dashboard-document-card--active/
    __meta div, .auth-summary-card, .auth-choice-card--active all layered an
    opaque white gradient over the surface -> solid white in dark mode. Added
    dark overrides (verified computed bg = rgb(23,32,28), no white gradient).
    Also fixed the hero .page-intro::before white glow in dark mode.
  [Archetype icons] The 6 archetype cards on /users now show distinct SVG icons
    (document / leadership / transition / product / data / communication) instead
    of 2-letter codes. Verified all 6 render an <svg>.
  [Extra pages] Reviewed about/contact/faq/privacy: consistent structure
    (PageScene + hero--centered + info-card/panel). Dark-mode hero glow fixed;
    panels/cards covered. Admin reviewed via code (auth-gated).

VERIFICATION
  - tsc --noEmit, node -c backend/api/index.cjs, terraform validate: pass.
  - terraform apply: 1 add (consultants group) + 1 change (Lambda), 0 destroy.
  - Cognito groups now: admin, consultants (+ IdP-generated Google/LinkedIn).
  - Archetype icons + dark-theme fixes verified in the local preview browser
    (isolated render: sidebar/documents/overview computed bg is dark).
  - npm run build clean.

NOT DONE / OWNER VERIFY
  - Logged-in dashboard visual pass needs prod (localhost can't hit the API due
    to CORS). Test account test@mentor.com (pw 123456789aA -> set
    PrincipnoPecataePi4) — to see the mentor dashboard, add it to the consultants
    group (command above) then log in.
  - Real end-to-end review of the mentor profile flow on prod.


==================================================================
SPRINT: PROFILE & ENGAGEMENT POLISH
==================================================================
Date: 2026-06-08 (later)

SHIPPED + DEPLOYED
  - GrowPoint Pro card: diagonal "coming soon" caution-tape ribbon; removed the
    payments-not-active line.
  - Notifications colour-coded by category (admin / message / booking / review /
    system) in the dashboard list and the header popover (shared helper).
  - Profile banner: confirmed it already fills its area (object-fit: cover,
    full-width); no change needed.
  - Profile layout: relocated the "browse other consultants" CTA into the left
    column and capped the booking list height; the empty gap beside the left
    column dropped from ~636px to ~264px (verified on a live profile via a
    temporary localhost API-CORS allowance, reverted afterwards).

VERIFICATION NOTE
  - Logged-in/profile pages can't be rendered from localhost (prod API CORS is
    www/CloudFront only). For this pass localhost was temporarily allowed through
    CORS to verify against the real dashboard/profile, then reverted (confirmed
    localhost is no longer allowed).

STILL OPEN (larger features, need a focused build with verification)
  - Social login: prompt user-vs-consultant role + prefill name/photo/city from
    the provider + a complete-profile step before finishing signup.
  - Interactive booking calendar (consultant picks hours / user books) replacing
    the slot list — this also fully closes the profile layout gap.
  - Dedicated /messages and /notifications pages (click-through from the popover).
  - "Предстоящи сесии" popup button for consultants and users.


==================================================================
SPRINT: ENGAGEMENT FEATURES + APPLE PREP (calendar, inbox, onboarding)
==================================================================
Date: 2026-06-09

SHIPPED (built clean: theme guard + tsc + vite all pass)
  - "Предстоящи сесии" is now a POPUP for both roles. The sessions panel became a
    modal opened from a top action button in the dashboard overview head and from
    the sidebar nav (moved out of the inline profile body). Shows the booking
    count.
  - Dedicated pages: /messages and /notifications (new routes + pages). The header
    notification/message popover items and "Виж всички" now deep-link to these
    pages instead of dashboard hash anchors. /notifications = full list with
    category colour tags + mark-all-read (syncs the header badge). /messages = a
    conversations-by-booking inbox (list + thread + reply) for confirmed sessions.
    Both gate to a login prompt when signed out. Verified rendering for both.
  - Interactive booking calendar (NEW shared component
    src/app/legacy/AvailabilityCalendar.tsx). A compact month grid used in TWO
    modes: "book" (user picks a day -> sees free hours -> selects) on the public
    consultant profile, and "pick" (consultant taps day+hour to add/remove a slot)
    in the dashboard availability composer. The old flat day-list is replaced; the
    weekly-pattern bulk composer is preserved under a collapsible <details>.
    Verified live on a public profile (today auto-selected, available days
    highlighted with a count badge, past days disabled).
  - Profile (D): top action buttons (Документи / Свободни часове [consultant] /
    Предстоящи сесии) in the dashboard overview head. The PUBLIC consultant
    profile now detects the owner (ownerUserId === current user) and shows an
    owner note plus "Редактирай" links per section (За консултанта, Експертиза и
    фокус, Резервация) and a top "Редактирай профила" button, all linking back to
    the dashboard editor. (The header name-chip still opens /dashboard as the hub,
    which prominently links to the public page; regular users have no public page.)
  - Social onboarding (#1): the role choice already existed; added a
    complete-profile step shown during provider sign-up — name/photo prefilled
    from the provider account (avatar preview), plus optional Град / Професия
    fields, passed into /auth/bootstrap.
  - Contact page: the three channels are contactus@ / partnership@ / legal@
    growpoint.bg (general / partnerships+ads / legal+data); the form routes to the
    matching address. (Inbound mail forwarding is ImprovMX — see README.)
  - Earlier in scope and already live: GrowPoint Pro "coming soon" tape, removed
    data-export ("Свали копие на данните си") from the profile (privacy policy
    covers it / request by email), ad box below ТАБЛО from assets/advertisement/,
    and the matches section ("най-добро съвпадение") only shows at 100% profile
    completion.

APPLE SIGN-IN (C) — CONFIGURED (Cognito live; frontend rebuilt, deploy pending)
  - Apple IdP created in Cognito (terraform apply: 1 add SignInWithApple + 1
    change adding it to the app client's supported_identity_providers). Key ID +
    Team ID + Services ID + .p8 all live ONLY in the gitignored terraform.tfvars.
  - IMPORTANT FIX: the Cognito web flow needs the **Services ID** as client_id,
    not the native Bundle ID. Corrected apple_client_id ->
    com.growpoint.growpointloginservice (the .../growpointlogin Bundle ID would
    have failed). The Services ID is the public client_id Apple sees in the
    authorize URL.
  - Verified live: GET /oauth2/authorize?identity_provider=SignInWithApple -> 302
    to appleid.apple.com/auth/authorize with client_id=...growpointloginservice
    and redirect_uri=.../oauth2/idpresponse, no redirect_uri/invalid_client error.
  - Frontend: VITE_COGNITO_SOCIAL_PROVIDERS=Google,LinkedIn,Apple; rebuilt and
    confirmed the value inlined into both the GitHub Pages and CloudFront bundles.
    The Apple button auto-enables (no longer "Скоро") once the new bundle deploys.
  - Remaining to publish: commit + push (GitHub Pages) and npm run deploy:cloudfront,
    then a real Apple sign-in once on the live origin.

SECURITY / CORS
  - Local logged-in/public verification used a temporary localhost entry in the
    API+S3 CORS allowlist; it was REVERTED and re-applied at the end of this pass.
    Confirmed prod CORS allow_origins is back to www.growpoint.bg + the CloudFront
    domain only (no localhost). No secrets are in tracked files.

STILL OPEN / OWNER VERIFY
  - Apple: real end-to-end Apple sign-in on the live origin after deploy (Cognito
    side already verified; see APPLE SIGN-IN above).
  - Logged-in visual pass on prod for: the sessions modal, /messages thread+reply,
    the consultant "pick" calendar, and the social complete-profile step (these
    are auth-gated; localhost can't hit the prod API once CORS is reverted).
  - Deploy: rebuild is clean locally; commit + push (GitHub Pages) and
    `npm run deploy:cloudfront` to publish, per the owner's deploy flow.


==================================================================
SPRINT: SHAREABLE MEMBER PROFILES + FULL CHECK
==================================================================
Date: 2026-06-09 (later)

SHIPPED
  - Shareable member profile for EVERY user. New public, link-only card at
    /u/:userId backed by a new public endpoint GET /public/users/{id}.
      * Backend (backend/api/index.cjs): getPublicUser returns ONLY safe fields
        (userId, name, role, avatar [signed], city, occupation, headline, bio,
        experienceSummary, experience/education highlights, skills, interests).
        Never email/age/goals/keywords/documents/plan/bookings. id = Cognito sub
        (a UUID) so the card is effectively unlisted — resolvable only via the
        share link. 60s public cache like /consultants.
      * Terraform: added route GET /public/users/{id} (no auth) + applied (Lambda
        repackaged, route created). Also added an ignore_changes lifecycle to the
        Apple IdP so plans no longer show a phantom in-place diff (matches the
        Google/LinkedIn pattern; private_key is write-only so it is ignored —
        rotate with `apply -replace`).
      * Frontend: new MemberProfilePage (/u/:id), api.getPublicUser, a
        PublicUserProfile type, applyMemberProfileSeo (noindex, ProfilePage OG +
        avatar). Dashboard overview now has a "Сподели профила" button
        (consultants -> /consultants/:slug, clients -> /u/:id) and a "Виж
        публичния си профил" link; the card shows an owner "Редактирай профила"
        button (-> /dashboard#profile-basics) when you view your own. Avatar has
        an initials fallback when no picture is set.

FULL CHECK (this pass)
  - Frontend tsc --noEmit ............................ PASS
  - npm run build (theme guard + tsc + vite) ........ PASS (Dark-theme check OK)
  - Backend node --check backend/api/index.cjs ...... PASS
  - terraform validate .............................. Success
  - terraform fmt -check -recursive ................. clean
  - npm run smoke:prod .............................. 14/14 PASS (run p0p78jtr90k)
  - CORS posture: localhost Origin -> NO ACAO header (rejected); www.growpoint.bg
    -> allowed. Prod is secured (no localhost).
  - GET /public/users/{unknown} -> handler 404 "Profile not found." (distinct
    from API-GW "Not Found", proving route+handler wired).
  - Apple: GET /oauth2/authorize?identity_provider=SignInWithApple -> 302
    appleid.apple.com (client_id = Services ID, correct idpresponse redirect).
  - Member page render: verified in the preview browser (route mounts; graceful
    "Профилът не е намерен" on API failure; happy path via a stubbed response
    renders name, SEO title, avatar initials, share button, 6 expertise chips,
    "Опит" + "Експертиза и интереси" sections; no console errors).

WHAT'S NEXT / ISSUES FOUND (minimalistic)
  PRIORITY | ITEM                                   | NOTE
  -------- | -------------------------------------- | ------------------------------
  P0       | Deploy frontend bundle                 | commit+push + deploy:cloudfront;
           |                                        | backend/Cognito/CORS already live
  P1       | Real Apple sign-in on live origin      | Cognito handoff verified only
  P1       | Logged-in visual pass on prod          | sessions modal, /messages reply,
           |                                        | pick-calendar, social step, member
           |                                        | owner-edit (auth-gated)
  P1       | SES transactional email sender         | blocked on DNS + SES prod access
  P2       | Rich social preview for /u/:id          | SPA OG only; needs static prerender
           |                                        | (currently noindex/link-only)
  P2       | Member card privacy opt-out toggle     | no opt-out flag yet (safe fields,
           |                                        | unlisted by UUID)
  P2       | Admin destructive-action browser QA    | approve/reject/delete/message
  P2       | Cold-start API latency (~1-2s)          | warmer/lighter path
  P2       | backend/api/node_modules committed     | repo hygiene

NOTHING BROKEN FOUND in the safe checks above. The only "issues" are the
deploy-pending state and the carry-over backlog items (SES, admin browser QA,
prerender for member OG, latency, repo hygiene).


==================================================================
POST-LAUNCH FIXES (owner feedback after going live)
==================================================================
Date: 2026-06-09 (post-launch)

Owner reported 3 issues after pushing live; all fixed:

  1) Social onboarding popup overflowed its border.
     - Cause: in the dashboard SocialOnboardingModal, grid/flex children could
       exceed the card width on narrow screens (fieldset default min-inline-size,
       grid children without min-width:0, and the long ghost-button label
       "Ще го направя по-късно" with white-space:nowrap).
     - Fix (global.css): .modal-card > * { min-width:0 }, modal inputs
       width/max-width:100% box-sizing, modal-card action buttons white-space:
       normal (wrap), .onboarding-avatar flex-wrap, and the new .onboarding-roles
       fieldset reset (border/margin/padding 0, min-inline-size:0).
     - Verified at 375px in the preview: card 16->359px, 0 overflowing children,
       no horizontal scroll (markup injected against the real CSS).

  2) Mobile "табло" moved while scrolling.
     - Cause: .dashboard-sidebar (and the profile/booking/faq asides) stay
       position:sticky in the single-column mobile layout, so they pin while the
       content scrolls.
     - Fix (global.css @media max-width:1120px): .dashboard-sidebar,
       .profile-stage__aside, .profile-aside-stack, .booking-card,
       .page-side-card, .faq-aside -> position:static; top:auto; max-height:none;
       overflow:visible. They now scroll with the page on mobile.

  3) "Виж публичния си профил" / role not assigned for social signups.
     - Cause: social logins always bootstrap role=client, and the onboarding
       popup never let them choose; a consultant-intended user stayed a client,
       so their public profile didn't match.
     - Fix:
       * Backend (bootstrapUser): added a `setRole` flag. Cognito group stays
         authoritative; otherwise a new user OR a request with setRole applies the
         chosen role, while routine bootstrap calls (no flag) keep an existing
         user's role (no silent demotion). Choosing "consultant" still creates the
         consultant draft. Deployed via terraform apply (Lambda updated; /health
         200).
       * Frontend: SocialOnboardingModal now shows the user/consultant choice
         (reuses the auth role cards), saves via bootstrap({ role, setRole:true,
         name, city, occupation }) then the avatar upload, and reloads on a role
         change so role/consultant-draft/nav/public-page links are consistent.
     - NOTE: full Cognito group sync (adding the user to the consultants/clients
       group) was NOT added — it needs the cognito-idp SDK + IAM + USER_POOL_ID
       env, which would bloat the committed backend node_modules. The DynamoDB
       role (authoritative in bootstrap) fully drives app behavior; group sync is
       deferred (groups remain an admin override). Carried in the backlog.

CHECK AFTER FIXES
  - npm run build (theme guard + tsc + vite) ... PASS
  - node --check backend/api/index.cjs ......... PASS
  - terraform plan/apply ....................... 1 in-place Lambda change, /health 200
  - Modal overflow + render .................... verified at 375px (preview)

DEPLOY STATE
  - Backend (setRole) is LIVE via terraform. The frontend CSS + modal changes are
    staged & built; commit+push (GitHub Pages) + npm run deploy:cloudfront to ship.


==================================================================
PAGE-BY-PAGE QA (web + phone) — owner asked to double-check everything
==================================================================
Date: 2026-06-09 (post-launch QA)

METHOD
  - Temporarily allowed http://localhost:5173 in the API+S3 CORS allowlist
    (terraform apply) to render real prod data locally, then REVERTED + applied
    (confirmed: localhost gets no ACAO header; www.growpoint.bg still allowed).
  - Swept every page at desktop (1280) and phone (375) in the preview browser:
    measured horizontal overflow, console errors, data load, tap-target sizes,
    and column balance. A persisted consultant session let me cover the
    auth-gated pages with live data.

PAGES CHECKED (all: 0 horizontal overflow, 0 console errors, data loads)
  Public: / , /users (11 cards), /consultants/:slug (calendar), /about, /faq,
    /contact, /terms, /privacy, /auth, /u/:id, 404.
  Authed: /dashboard (consultant), Предстоящи сесии modal, /messages,
    /notifications, /account. Dark mode: surfaces dark, no light leaks.
  Phone: consultant calendar day cells 41x41px, hour slots 44px tall (good touch
    targets); modals stay within the viewport; sidebar no longer sticky.

ISSUES FOUND + FIXED
  [FIXED] Desktop consultant profile had a large empty area bottom-left: the
    right column (snapshot + booking calendar) ran ~568px taller than the left
    (bio + expertise + CTA). Moved <HowItWorksCard/> from the right aside into the
    left column (before the "Разгледай и други" CTA). Columns are now balanced
    (left 1224px vs right 1228px -> 5px gap). Reads fine in the learn-flow; on
    phone it simply appears before the booking widget.
  [FIXED] Dashboard greeting showed "Добре дошъл, ." when the profile name was
    empty (dangling comma+period). Now renders "Добре дошъл." with no name, and
    "Добре дошъл, <Име>." when a name exists. (firstName also trimmed/guarded.)
  [CONFIRMED FIX] Mobile "табло": .dashboard-sidebar computes position:static on
    phone (the sticky-on-mobile bug from the previous pass is resolved).

NOT A BUG (observed)
  - A few footer/inline <a> links are ~20-24px tall on phone (acceptable for text
    links; primary actions/calendar/slots are all >=40px).
  - The QA consultant account has an empty name and 4% completion — that's why the
    greeting edge case surfaced; real accounts have names.

CHECK AFTER FIXES
  - npm run build (theme guard + tsc + vite) ... PASS
  - CORS reverted + verified (localhost rejected, www allowed)

DEPLOY: frontend changes (gap rebalance + greeting guard) are staged & built;
  commit+push (GitHub Pages) + npm run deploy:cloudfront to ship.


==================================================================
CI: GITHUB ACTIONS — SECRET SCAN + BUILD GATE
==================================================================
Date: 2026-06-09 (CI)

ADDED .github/workflows/ci.yml (runs on push to main, pull_request, manual):
  - secret-scan job (HARD GATE so no sensitive info gets pushed):
      * scripts/check-secrets.sh (npm run check:secrets) — fails if:
        - a secret-bearing file is tracked: terraform.tfvars(.json), .env/.env.*
          (except the public .env.production), *.p8/*.pem/*.key/*.p12/*.pfx,
          id_rsa* (guards against `git add -f`);
        - high-signal secret values appear in tracked source: GOCSPX- (Google),
          WPL_AP1. (LinkedIn), AKIA/ASIA… (AWS), BEGIN … PRIVATE KEY,
          aws_secret_access_key=, xox?- (Slack);
        - .env.production gains a secret-looking entry;
        - .gitignore loses a required rule (*.tfvars/*.p8/*.pem/*.key/.env).
        Excludes docs (*.md, test.txt), *.example, package-lock.json, vendored
        node_modules, dist/, assets/.
      * gitleaks (gitleaks/gitleaks-action@v2 + .gitleaks.toml) — generic/entropy
        secret detection over the pushed/PR commits; vendored deps, build output,
        docs and templates allowlisted.
  - build job: npm ci + npm run build (theme guard + tsc + vite) + node --check
    backend/api/index.cjs.
  - terraform job: fmt -check + init -backend=false + validate (no AWS creds).

TESTS RUN LOCALLY
  - scripts/check-secrets.sh on the clean tree -> PASS (exit 0).
  - Negative test: force-added a file with GOCSPX-… and AKIA… -> guard FAILED
    (exit 1) listing the hits; cleaned up -> PASS again.
  - Negative test: force-added a __*.p8 -> guard FAILED on tracked secret file;
    cleaned up -> PASS.
  - ci.yml: no tabs, all three jobs present; .gitleaks.toml valid TOML.

NOTE: gitleaks-action is free for personal accounts / public repos (only GitHub
  *organizations* need GITLEAKS_LICENSE). If the repo ever moves to an org, set
  that secret or switch to the gitleaks binary; the custom guard stays the
  authoritative project-specific gate either way.
