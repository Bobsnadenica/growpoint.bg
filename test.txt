GrowPoint — QA & change log
===========================
Last updated: 2026-06-14. Public file — no secrets here.
Deep docs: README.md · agent/dev notes: CLAUDE.md · upcoming work: plan.txt

Targets
  - Production site (GitHub Pages):  https://www.growpoint.bg
  - CloudFront test domain:          https://d30m6jtjij7col.cloudfront.net
  - API:                             https://zmajj05nm1.execute-api.eu-west-1.amazonaws.com

How to test (gates — all must pass before shipping)
  npm run build                  # theme check + tsc + vite + route copies
  bash scripts/check-secrets.sh  # secret scan
  node --check backend/api/index.cjs
  npm run smoke:prod             # 14 read-only production checks -> expect 14/14
  terraform -chdir=infra/terraform validate   # for infra changes (review `plan` before apply)

Verified working (as of 2026-06-14)
  - Build + secret scan + tsc + backend syntax: PASS. smoke:prod 14/14.
  - Security: no XSS sinks; all /me, /bookings, /admin endpoints reject anonymous (401);
    auth via Cognito JWT authorizer; IAM scoped to the 3 tables + CV bucket + SES +
    read-only/enable-disable Cognito; secrets only in gitignored tfvars.
  - Auth: email + Google + Apple + LinkedIn (Cognito hosted UI).
  - Paid model: Start 9.99 / Grow 29.99 / Spotlight 99.99 EUR/mo, clients free,
    buttons "Очаквай скоро" (Stripe pending). Badges: Grow / ★ Spotlight.
  - Mentor onboarding invite-only (admin email invite -> free comped account);
    self-serve consultant signup blocked with a notice. No approval step.
  - Admin: invite, restrict/suspend (hides profile + disables Cognito login),
    message, grant package, feature. Monitoring dashboard reads real Cognito + DB.
  - Data: 3 DynamoDB tables growpoint-dev-{users,consultants,bookings} (11 public
    consultants), CV/media in growpoint-dev-cv-* bucket.
  - Responsive (1280 + ~390) with 0 horizontal overflow; dark theme clean; logo
    light/dark variants verified.

Open items (details + ready-to-run prompts in plan.txt)
  P0  Stripe checkout — enables self-serve paid mentor signup.
  P0  SES production access — account is in sandbox, so platform email only reaches
      verified addresses. growpoint.bg DNS is in; request production in AWS console.
  P1  Enforce `restricted` per request (Cognito disable blocks new logins; an
      existing token stays valid ~1h).
  P1  CloudFront DNS cutover (jethost.bg) — also adds HTTP security headers.
  P2  Delete old careerdoc-dev-* tables/buckets once the rename is confirmed stable.

Change history (newest first)
  2026-06-22  Points/rewards (clients only) + booking pay-gate. Earn: profile 100%
              =20, referral (friend completes profile) =30, attended+confirmed
              consultation =10, review =10. Spend: 100 points = 1 free consultation
              (applied at booking; refunded if declined/cancelled). Atomic +
              idempotent (DynamoDB conditional updates). Booking flow: consultant
              adds a meeting link on/after approval; the client sees it only once
              paid — free-via-points releases it immediately, otherwise it's gated
              ("Stripe скоро") with an admin manual "mark paid" bridge (/admin →
              Чакащи плащане). Referral link ?ref=CODE; dashboard PointsCard
              (balance, progress, referral, history). Backend live + verified
              (new endpoints 401 anon); build/tsc/secret-scan PASS; PointsCard
              DOM-verified. Next: Stripe replaces the manual mark-paid.
              Follow-up same day: referral "invite a friend" link is always shown
              (getMeProfile backfills referralCode for older accounts) with
              Copy + Share (Web Share API); PointsCard now shows a profile-
              completion checklist (the 6 fields) with the missing ones glowing
              amber + "Допълни профила" link.
  2026-06-14  Logo: header uses logo.svg wordmark (light + dark variants), dropped
              the separate "GrowPoint" text. Docs simplified (README/CLAUDE/test).
  2026-06-14  Paid-mentor model: all tiers paid (no free), email invites -> comped
              accounts, admin restrict/suspend (Cognito disable), approval removed.
              Pricing Start reverted to 9.99 EUR/mo.
  2026-06-13  Rename careerdoc -> growpoint across infra + code; zero data loss /
              zero downtime (table + bucket migration, terraform state reconcile).
              Cognito pool name left pinned (immutable).
  2026-06-13  Admin monitoring made authoritative — reads real Cognito user pool
              + DynamoDB (registered vs activated, by login provider, drift).
  2026-06-13  Owner round: header notif popup parity, fancier Spotlight badge,
              auth heading, social-onboarding popup polish.
  2026-06-12  Full audit: gates + drift clean; security review; both-role browser
              sweeps; fixed owner-edit links + pending-profile panel on live.
  2026-06-11  Notifications: full-text popup, per-item mark-read, booking action
              opens the sessions popup.
  2026-06-10  Overlay portal fix (modals/lightbox), age 18–95, /files page,
              ad rotation; Sprint R1 (copy / области / packages / pricing / badges).
  2026-06-09  Sessions popup; /messages + /notifications pages; booking calendar;
              shareable member cards; Apple sign-in configured.
  2026-06-08  Google + LinkedIn login live; social onboarding modal.
  2026-06-07  CloudFront per-route SEO + function; API cache headers.
  2026-06-06  Initial live QA: public reads, auth, bookings, messages.
